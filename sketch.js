function setup() {
  createCanvas(1000, 800);
   background("white");
}

function draw() {
  
  
  fill ("red");
  stroke ("lightblue");
  
  if (mouseIsPressed); {
  circle (mouseX,mouseY,30);
  }
}
